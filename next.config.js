/** @type {import('next').NextConfig} */
const nextConfig = {
  assetPrefix: '/Portfolio-I-Talent-Ruben/',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
}

module.exports = nextConfig
